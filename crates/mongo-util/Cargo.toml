[package]
name = "mongo-util"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
mongodb = { version = "2.4.0", default-features = false }
tracing = { version = "0.1.37", default-features = false }